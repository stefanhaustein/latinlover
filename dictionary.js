goog.provide('dictionary');


/**
 * @constructor
 * @param {
 */
dictionary.Dictionary = function() {
  this.processor = processor;
}

/* @param {string} text
 * @param {function(grammar.Wort,string)} processor
 */
dictionary.Dictionary.load()